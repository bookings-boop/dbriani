# ── Secrets — never commit ──────────────────────────────────────
.env
.env.*
!.env.example

# Runnable workflow copies with secrets inlined (build_workflow.py output)
*.local.json

# Known-paid-customer lookup table — contains customer phone + revenue (PII).
# Regenerated by scripts/build_known_customers.py; shipped to the box out of
# band (scp), NEVER committed.
hermes-bridge/known_customers.json

# ── OS / editor / build noise ───────────────────────────────────
.DS_Store
*.log
__pycache__/
*.pyc
.venv/
venv/
node_modules/

# backups / live snapshots
*.bak.*
*.LIVE-backup-*
*.bak.pre-*
*.PRE-*
