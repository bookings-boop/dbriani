# Dubriani audit — 2026-05-28 (post-incident)

Status legend: ✅ fixed+live · ⏳ fix ready, awaiting quiet-window deploy · 🔬 needs live trace

## ✅ Fixed and verified today
| # | Issue | Root cause | Fix |
|---|---|---|---|
| 1 | Editing didn't work / replies misrouted to /assist / **wrong-number send** | n8n container missing `BRIDGE_TOKEN` → every code-node bridge call (edit-lock, get-by-tgmsg, awaiting, paylink-amount) got **401** → silently "not found" | Wired `BRIDGE_TOKEN` into ~/n8n/.env + docker-compose, recreated n8n |
| 2 | **[📎 Send File] did nothing** | Route Action rule[15] read `$json.action` (overwritten by Disarm Autosend) vs the other 15 rules' `$('Parse Callback').item.json.action` | Fixed rule[15] leftValue |
| 3 | Regenerate / auto-improve: sent ≠ shown | card edited but Redis (what Send reads) not updated atomically | atomic `regen-commit` bridge action; both branches + improve flow routed through it |
| 4 | /assist could nudge/send to WRONG customer with no way to catch it | fuzzy name match + card showed only name/@lid | show resolved **phone + confirm**; send_file now gated behind a button; new-number → phone lookup or /send guidance |

## ✅ Also fixed after report
| 5 | **File button double-send** when tapped repeatedly | file-send had no rapid-tap idempotency | bridge `SET NX filesend:<cid>:<file_key>` 60s in handle_send_file — verified (2nd rapid call → deduped). Bridge-only, no n8n restart |

## ⏳ Fix ready — deploy in a quiet 60-sec window (each needs a bridge or n8n restart)
| # | Issue (severity) | Root cause | Proposed fix | Risk |
|---|---|---|---|---|
| H | **Card doesn't show "SENT" / buttons stay tappable after send** (HIGH — causes the re-tapping) | `Edit Telegram (Send)` changes text but has no `reply_markup` → Telegram keeps the inline keyboard; file path uses a *new* sendMessage and never touches the card | add `"reply_markup": {"inline_keyboard": []}` to Edit Telegram (Send); disable the card's buttons on file send too | low (cosmetic post-send edit) |
| B | **"book the yacht" not recognized → stays WARM** (HIGH — wrong analysis "wait 48h" + customer not on top of /review) | `LETS_DO_IT_RE` matches "book it" but not "book the …"/"book now" | broaden regex: `book\s+(it\|the\|this\|that\|now\|a\|my\|us\|me\|asap)` | low |
| C | **Slow / unresponsive buttons during live sessions** (HIGH) | hourly background "sweep" fires ~9 lead-analysis Hermes calls at once on a 2-vCPU box → 42s queue waits | cap background Hermes to 1 (interactive always gets ≥2 of 3 slots); optionally rate-limit the sweep | low |
| D | **Refine (edit) is non-atomic card↔Redis** (MEDIUM — latent sent≠shown) | `Parse Refine → Sync Refine To Redis (Redis first) → Edit Telegram (Refine)`; crash between leaves Redis ahead of card | route refine through `regen-commit` (card-first, atomic) | med (touches the just-fixed edit flow — test carefully) |

## 🔬 Needs a live trace / careful pass (not a quick patch)
| # | Issue | Hypothesis | Next step |
|---|---|---|---|
| E | Draft "forgets" the date the customer already gave / forgets the just-sent message | WAHA history sync lag — the latest customer/Dubriani messages aren't in the fetched history when the draft is built (Format Context uses `Get Chat History` last-15) | trace one live draft: compare WAHA history at draft time vs actual thread; consider seeding history from the debounce buffer + last sent draft |
| F | Native file attachment (currently sends a Drive **URL**) | WAHA on CORE (free) tier blocks native media | buy WAHA Plus → native attachments work, no code change |
| G | Box capacity | 2 vCPU can't run hourly sweep + live high-volume chat together | upgrade to 4 vCPU (real fix for sustained volume) |

## Recommended deploy order (one restart each, quiet window)
1. **C** (sweep cap) — biggest stability win; makes buttons responsive → fewer double-taps.
2. **A** (file dedup) + **B** (booking regex) — bundle into one bridge deploy.
3. **D** (refine atomic) — separate, test edit thoroughly after.
4. **E** — dedicated investigation with the headroom from C.
