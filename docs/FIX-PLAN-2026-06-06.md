# Dubriani/Hermes — Master Fix Plan (2026-06-06)

Covers 10 operator-reported bugs (A–J) + Requirement R1 (location/confirmation), mapped to 6 subsystems.
Companion RCA: `docs/draft-pipeline-rca-2026-06-06.md`. Spec: `docs/confirmation-and-location-templates.md`.
**Status: NO code changed. Read-only investigation complete for A,B,C,D,E,F,G,H + R1; I,J confirming.**

---

## A. WHAT'S PENDING (blockers before I can start fixing)

1. **Owner decisions (the #1 blocker)** — ~18 policy/ground-truth calls grouped in §D. Several are cross-cutting (autonomy, price enforcement, bareboat). Implementation of Phases 1–4 is gated on these.
2. ✅ I & J done. All 10 bugs + R1 root-caused.
3. 🔒 **LEAK CONFIRMED reached the customer twice** (Bug I — internal "Zayn" note, 06-05 20:02/20:03, via a file-send caption that bypasses the scrub) → elevated to **first code hotfix (Phase 1A)**.
3b. ⚠️ **Catering price contradiction to resolve:** you said Fine dining **2,500/person** + BBQ **1,500/2pax**; the prompt says Fine dining **2,500 for 2** + BBQ **2,500 for 2 (min 1,500)**. Need your authoritative numbers (decision §D.4).
4. **Pre-flight drift reconcile** (read-only, ~1–2h): diff box vs git-HEAD vs local working tree for `review.py/labels.py/routes.py/server.py`; decide how to handle the uncommitted LOST-label edits; run `drift_check_catalog.py`; confirm box==git for files we'll touch.
5. **Intake diagnostic (Bug H)** — a deeper read-only probe to pin the exact silent-drop point (WAHA session vs n8n webhook filter).
6. **Immediate OPERATIONAL (non-code, today):** reply to Antonio (+971588211789) with address/berth + the canonical confirmation (viewing is tomorrow); manually reach out to Ayaan (+971589954694, lost lead).

---

## B. SEQUENCING PRINCIPLE (why this order)

On a LIVE financial system, order by **(1) stop active harm I can fix safely → (2) reversibility/blast-radius → (3) dependency**:
- **Display-only first** — can't break sends/payments/customers, fully reversible, and it immediately recovers revenue (buried ready-to-book leads) and removes dead leads from the top.
- **Deterministic guards next** (scrub, price/pin validators) — contained, stop the confidentiality leak + customer-facing misinformation.
- **Prompt / draft-quality after** — operator still approves every draft, so moderate risk.
- **Behavior & lifecycle last** (auto-close, auto-LOST, cadence, auto-send) — highest judgment + most owner decisions + can mis-handle live leads.
- **Schema/data writes & infra interleaved where they unblock**, riskiest data writes last.
- **Hard dependencies:** catch-all (C-Fix2) BEFORE deploying the LOST label; LOST deployed BEFORE B's auto-LOST routing; drift reconcile BEFORE any bridge edit; ONE coordinated `review.py` change for C+D+E (shared file).

---

## C. THE PLAN (phased)

### PHASE 0 — Pre-flight + operational (today; ~0.5 day + your decisions)
- Operational: send Antonio his confirmation/berth (I'll draft from R1); manual outreach to Ayaan.
- Read-only: drift reconcile; LOST-edit sequencing decision; `drift_check_catalog.py`; B-Fix0 cron diagnostic; intake diagnostic (H).
- Collect owner decisions (§D).
- **Deploy surface:** none. **Risk:** none.

### PHASE 1A — LEAK HOTFIX ✅ SHIPPED 2026-06-06 (commit 7221099, deployed + verified live)  ·  done
The internal "Zayn" operator note reached a customer **twice**. Add a send-boundary scrub that strips internal notes/names/bracketed instructions/rule-citations on **EVERY** outbound path — especially the **file-send caption / sendText-fallback** path that bypasses `sanitize_draft_messages` today.
- **Deploy:** bridge. **Tests:** new scrub tests (internal-note/name/caption stripped; legit caption preserved). **Risk:** LOW-MED (must not over-strip legit content). *Exception to "display-first" ordering because it's a live confidentiality breach and is contained.*

### PHASE 1 — Lead visibility & sort (review.py) — ✅ COMPLETE 2026-06-06 (all DEPLOYED+LIVE)
- catch-all + LOST label deploy — `51f2af4`
- C-Fix1 passed-event guard (Emilie off top) — `d73e118`
- C-Fix3a EV-sort by potential value — `fd4dbb8`
- C-Fix3c `/review <tier>` uncap (every lead reachable) — `240fc1f`
- D-Fix CONFIRMED detail in AWAITING (Antonio paid/booking visible) — `8844efb`
79 unit tests green; box == HEAD. Deferred: importance-floor for default view; D-Fix2 visit-reason + D-Fix3 confirmation-sent flag (→ Phase 4).
Recovers revenue immediately; display-only & reversible. ONE coordinated `review.py` change covering C + D + E.
- **C-Fix2** catch-all (no lead can vanish) → then **deploy the queued LOST label** safely.
- **C-Fix1** passed-event guard → dead Emilie off the top.
- **C-Fix3 + E-vis** EV-sort the main list + fix `/review hot` uncap + importance floor → surfaces Milenski & Emma.
- **D-Fix1/2** CONFIRMED detail renders in AWAITING (Antonio shows booked/paid/time/visit-reason + 🔴).
- **Deploy:** bridge (`deploy_bridge.py`). **Tests:** test_render_review, test_awaiting_section, test_review_owe_and_close. **Risk:** LOW (operator-facing output only).

### PHASE 2 — Fabrication guards — ENFORCEMENT LAYER ✅ LIVE; n8n polish remaining
SHIPPED 2026-06-06 (bridge-only, no n8n mutation): pin guard (`7112886`) + deterministic price validator wired to quality-check hard-block→regen (`4f56ee0`). Wrong prices/pins can no longer reach a customer regardless of prompt. REMAINING (n8n-side polish, reduces regen frequency + the confirmation message): catering BBQ text fix, purge 6 stray pins, deterministic confirmation template.
Highest-severity *content* problems; deterministic, contained. (Leak scrub already shipped in 1A.)
- **A-S2 + I-catering + R1-pin** deterministic price/spec/pin validator + per-yacht/per-item fact card + single canonical config (yacht + catering prices + the 2 pins). Kills "600", "375", wrong/fake pins. *(Recurring 375 hallucination since 2026-06-01 proves soft prompt guards aren't enough → must be deterministic.)*
- **R1** deterministic booking-confirmation template (your exact format) + purge the 6 stray pins from few-shot examples.
- **Deploy:** bridge + n8n + catalog (`drift_check_catalog.py` after). **Tests:** upgrade test_no_invent to validate prices; new validator tests. **Risk:** MED (financial output; choose hard-gate vs soft-flag — §D).

### PHASE 3 — Draft quality & conversation correctness  ·  ~3–4 days
Operator still approves every draft → moderate risk. Depends on Phase 2 grounding.
- **A-S1** rebuild the `Apply Improvement` card (improvement replaces in place, not appended).
- **A-S3 + E-bubble** STYLE/TONE into rewriters + hard bubble-count cap (`sanitize_draft_messages`) + scorer verbosity penalty → Ritz-Carlton concision, stop overtexting fan-out.
- **A-S4** capacity-fit respects the chosen yacht (no upsell-off-their-pick).
- **F** first-contact stage gate (greet → get name → then options) + name source = asked name.
- **I-memory** prior outbound/quotes reach drafter+analyzer + "never contradict a prior quote."
- **J** debounce/consolidate consecutive inbounds + supersede prior open draft.
- **Deploy:** n8n + bridge. **Tests:** test_capacity_fit, test_style_directive, test_quality_*, test_dedup/queue. **Risk:** MED.

### PHASE 4 — Analysis engine + lifecycle (rejection→close, reengage, cadence)  ·  ~3–4 days
Highest-judgment behavior changes; most decisions; depends on LOST (Phase 1).
- **B** rejection detection (lexicon + regex) + service-mismatch rule + recency de-weight + re-score on decline + graceful-goodbye draft → close → LOST.
- **G** reengage correct population + cadence reconciliation with E (overtexting) + cold_lastshot fix.
- **D-Fix3** confirmation/berth-sent tracking + ⚠️ "not sent" flag.
- Reconcile the open **auto:analyzer_close rein-in** vs B's "fire more for genuine declines."
- **Deploy:** bridge + n8n. **Tests:** test_score_lead, test_reengage, test_close_bucket, test_cold_decay, new golden tests. **Risk:** MED-HIGH (could mis-close live leads; keep approval-gated until proven).

### PHASE 5 — Intake reliability (Bug H, silent drops) — CRITICAL but needs diagnostic  ·  ~1.5–2 days
- Pin the drop point (WAHA session continuity vs n8n webhook filter), then add an **intake dead-letter/audit log** so a dropped inbound is never invisible again (+ alert).
- **Deploy:** WAHA/n8n/infra. **Risk:** MED (infra). *Diagnostic starts in Phase 0; fix lands here.*

---

## D. OWNER DECISIONS (grouped — these unblock the build)

**Autonomy / safety (cross-cutting):**
1. Keep everything **approval-first** (you tap to send) through all phases? (Strongly recommended.)
2. Price/pin enforcement: **HARD gate** (block/regenerate on any catalog mismatch) vs **soft flag** for your review?

**Pricing ground truth:**
3. Von Dutch 40 = **AED 1,400/hr flat, no discount** — confirm? Any sanctioned discount?
4. ⚠️ **Catering price CONFLICT — your numbers vs the system's:** you said Fine dining **2,500/person (min 2,500)** + Premium BBQ **1,500/2pax (min 1,500)**; the prompt (`system-prompt.md:452-456`) says Fine dining **2,500 for 2** + Premium BBQ **2,500 for 2 (min 1,500)**. Give me the ONE authoritative table. Was the "dinner for 2 from AED 2,500" we sent Xeno correct? Kill "375" (hallucination) — confirmed.
5. "Rule 39 / AED 2,000 Satoshi discount" — real or hallucination to suppress?

**Service / policy:**
6. Does Dubriani offer ANY bareboat/no-captain? (All evidence: crewed-only.)
7. Capacity overflow (party > chosen yacht): lead-with-their-pick+note vs +1 alternative vs hard-switch?
8. Is "Zayn" an internal name that must NEVER be sent to customers?

**Lead lifecycle:**
9. Soft "No thanks": auto-demote vs flag-for-your-confirm?
10. Declined/mismatch lead → 💔 LOST (new "service-mismatch" reason) vs NOT_A_CUSTOMER? (First auto-write to LOST — OK?)
11. Reconcile: should `auto:analyzer_close` fire MORE for genuine declines (vs the open rein-in item)?

**Follow-up cadence (reconciles overtexting ↔ reengage):**
12. Confirm reengage targets "we replied → customer went silent."
13. Cadence numbers: 1st nudge at 24h, last-shot ("given up?") 3–14d, cooldown 48–72h, max 2 nudges, stop at +7d? Propose-only (you approve)?

**Drafting / UX:**
14. Bubble ceiling per reply (rec 3–4; inclusions=1 bubble, options=1/yacht)?
15. First contact: hard-block options on the first reply vs require greet+name then allow after one exchange?
16. Switch personalization to the in-chat asked name (demote WhatsApp profile name to fallback)?

**Review / display:**
17. `/review hot` uncap/paginate vs importance floor? HOT+imp≥80 bypass the rate-first sort/cap?
18. Emilie (passed-event CONFIRMED) → CONFIRMED "won" vs NO ACTIVE SALE?

**Data / infra:**
19. Add intake dead-letter/audit log (H)? Link @lid↔phone identities (Emma; Antonio's two rows; Qurbani un-merge)?
20. Berth/gate value source: operator at confirm-time vs a booking field?

---

## E. TIME ESTIMATE (done properly, on a live system)

| Phase | Focused build+test | Notes |
|------|-----|-------|
| 0 Pre-flight + operational | 0.5 day | + your decision turnaround |
| 1A 🔒 Leak hotfix | 0.5–1 day | confidentiality — do first |
| 1 Visibility & sort + LOST | 1.5–2 days | visible improvement fast |
| 2 Fabrication guards + confirmation tmpl | 2–3 days | critical safety |
| 3 Draft quality + dedup (incl. J) | 3–4 days | |
| 4 Analysis + lifecycle | 3–4 days | most decisions |
| 5 Intake reliability (H) | 1.5–2 days | diagnostic from Ph0 |

- **Focused build+test effort: ~13–18 working days.**
- **Realistic calendar (staged deploys + 24–48h soak between risky phases on a live financial system + your decision turnaround): ~3–4 weeks.**
- **Fast wins:** the leak is closed **day 1**; buried-lead recovery (Phase 1) visible in **2–3 days**; full critical-safety layer (leak + fabrication + confirmation) within **~1 week** — if decisions are quick and we deploy in stages.
