# Handoff — 2026-05-21

End-of-night state. The Hermes integration build is **complete and deployed**;
H7–H9 behavioural testing is **paused to tomorrow**. `overnight-build` is
**not** merged — it stays on the branch until H7–H9 passes.

---

## ✅ What's working (verified)

- **Drafting via Hermes** — verified by a live `/draft` (clean draft, 7.4s,
  full field contract: `health`, `auto_send`, `conversation_mode`, …).
- **"Dubriani Hermes" bot** is the live approval/refinement surface; the old
  `8569` bot is retired (webhook deleted).
- **53-node workflow** live and active in n8n.
- **Refinement loop** (Step 4) — operator-confirmed ("both passed").
- **Rule injection** (5.2) — verified (a test rule shaped a draft).
- **Trigger detection** (6) — verified; **reminder cron** installed
  (every 15 min, quiet-hours aware 22:00–08:00 Dubai).
- **Health scoring** (7) — verified (`at_risk` / `warm` scored + persisted).
- **Autonomous mode** (8) + **§5.7 safety caps** — bridge-side fully verified:
  `set-mode`, `/manual` kill switch, all 3 caps (checkpoint, daily, sampling),
  `/caps`. Defaults to `approval`; nothing auto-activates.
- **Daily digest** (9) — cron installed (08:00 Dubai); a test digest delivered.
- **Services** — `hermes-bridge` + `hermes-gateway` (systemd user units, linger
  on), n8n / postgres / waha / caddy / redis containers — all running, left
  as-is.
- **Anthropic auth** — the mid-build outage is resolved (operator disabled
  `~/.claude/.credentials.json`, moved to API-key credentials).
- **Data integrity** — checked: no data loss; the 52-entry draft queue intact.

## ⏳ What's pending

1. **H7–H9 behavioural tests** (autonomous mode auto-send + caps) and
   **H3–H6** (rule capture button, trigger reminder) — need the operator's
   phone. Plans: `docs/h7-h9-test-plan.md`, `docs/test-cases-hermes.md`.
   *Structurally verified but not behaviourally tested:* the workflow's
   `IF Autonomous → Auto-Send to Customer (WAHA)` path.
2. **Merge `overnight-build` → `main`** — only after H7–H9 passes.
3. **Rotate exposed secrets** — `N8N_API_KEY` and the admin bot token were
   pasted in chat during the build.
4. **Autonomous-mode break conditions** (auto-revert on negative sentiment /
   hard-stop topics) — not built; caps are. See `docs/autonomous-mode.md`.
5. **Item 2** of the operator's "two things" — never received (item 1, the
   data-loss check, is done).

## ▶️ Do this first tomorrow

1. **Run H7** — from a test phone you control, message the Dubriani WhatsApp
   number. Confirm a draft card reaches the "Dubriani Hermes" Telegram bot.
2. Work through **H7 → H8 → H8b → H9** in `docs/h7-h9-test-plan.md` (Claude can
   watch the execution data + bridge logs live as you go).
3. If all pass → **merge `overnight-build` into `main`**.
4. **Rotate** `N8N_API_KEY` and the admin bot token.

## Reference

- Architecture: `docs/hermes-architecture.md`
- Decisions + blocker log: `docs/decisions.md`
- Autonomous mode (incl. unbuilt break conditions): `docs/autonomous-mode.md`
- Test plans: `docs/test-cases-hermes.md`, `docs/h7-h9-test-plan.md`
- Overnight summary: `docs/overnight-build-summary.md`
- Rollback: `workflows/phase-1b-telegram.LIVE-backup-*.json` — `PUT` one back
  via the n8n REST API (`scripts/deploy_workflow.py` shows the call pattern).
- Caps config: `~/hermes-bridge/.env` (`CAP_*`); inspect live with `/caps`.
